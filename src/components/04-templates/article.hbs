{{render '@header'}}
<main>
    {{render '@h1'}}
    {{render '@paragraph' paragraph="coucou"}}
    {{render '@figure'}}
    {{render '@paragraph' }}
</main>
{{render '@navigation'}}
{{render '@footer'}}
